version: 2
jobs:
        build:
                docker:
                        - image: circleci/node:4.8.3
                steps:
                        - checkout
                        - run: echo "Hello World!"
